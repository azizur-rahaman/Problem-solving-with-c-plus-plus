#include<bits/stdc++.h>
using namespace std;

void solved(){
  float a,b;
  cin>>a>>b;

  cout<<"floor "<<a<<" / "<<b<<" = "<<floor(a/b)<<endl;
  cout<<"ceil "<<a<<" / "<<b<<" = "<<ceil(a/b)<<endl;
  cout<<"round "<<a<<" / "<<b<<" = "<<round(a/b)<<endl;
  
  
}

int main(){

    int t=1;
    // cin>>t;

    while (t--) solved();

}