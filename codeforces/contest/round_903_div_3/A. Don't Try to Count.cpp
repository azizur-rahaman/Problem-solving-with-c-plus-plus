#include<bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin.tie(0);

    int t;
    cin>>t;

    while (t--)
    {
        int x,m,xt,xm, i=1;
        cin>>x>>m;

        xt=x;
        xm=m;
        while (xt--)
        {
            char temp;
            cin>>temp;
        }

        while (xm--)
        {
            char temp;
            cin>>temp;
        }

        
        
    }
    
    
}